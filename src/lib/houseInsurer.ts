/**
 * This system belongs to a licensed micro-insurer, which underwrites its
 * own business and takes clients direct.
 *
 * So there is no such thing as choosing an insurer here: every client and
 * every policy is ours by licence. The screens used to offer a picker,
 * which invited staff to record a policy against somebody else — a
 * question this product has no business asking, and an answer that would
 * be wrong on every document it later printed.
 *
 * Anything needing the underwriter's name reads it from here.
 */
export const HOUSE_INSURER = 'Motions'
